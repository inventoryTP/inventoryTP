import streamlit as st
import gspread
from google.oauth2.service_account import Credentials
import pandas as pd

# 1. เชื่อมต่อ Google Sheets
scope = ["https://www.googleapis.com/auth/spreadsheets", "https://www.googleapis.com/auth/drive"]
creds = Credentials.from_service_account_file("key.json", scopes=scope)
gc = gspread.authorize(creds)

# ตั้งค่าหน้าเว็บ (ต้องไว้บนสุด)
st.set_page_config(page_title="TP2025 Dashboard", layout="wide")

# --- ส่วนเมนู Sidebar สำหรับแยกหน้า ---
st.sidebar.title("📌 เมนูหลัก")
page = st.sidebar.radio("เลือกหน้าจอ:", ["📊 วิเคราะห์ยอดขาย", "📦 สต็อกสินค้าคงเหลือ"])

try:
    # =========================================================
    # หน้าที่ 1: วิเคราะห์ยอดขาย (ดึงไฟล์ ทีพี2025)
    # =========================================================
    if page == "📊 วิเคราะห์ยอดขาย":
        st.title("📊 ระบบวิเคราะห์ยอดขาย ทีพี2025")
        
        sh = gc.open("ทีพี2025")
        worksheet = sh.worksheet("แปลงข้อมูลยอดขาย")
        data = worksheet.get_all_records()
        df = pd.DataFrame(data)

        if not df.empty:
            date_col, id_col, name_col, qty_col, sales_col = 'วันที่สั่งซื้อ', 'รหัสสินค้า', 'ชื่อสินค้า', 'จำนวนที่สั่งซื้อ', 'รวมเงิน'

            # แปลงข้อมูลตัวเลข
            df[qty_col] = pd.to_numeric(df[qty_col].astype(str).str.replace(',', ''), errors='coerce').fillna(0)
            df[sales_col] = pd.to_numeric(df[sales_col].astype(str).str.replace(',', ''), errors='coerce').fillna(0)

            # ค้นหา
            search_term = st.text_input("🔍 ค้นหารหัสสินค้า หรือ ชื่อสินค้า:")
            if search_term:
                df = df[df.astype(str).apply(lambda x: x.str.contains(search_term, case=False)).any(axis=1)]

            # ยุบรวมข้อมูล
            summary_df = df.groupby([id_col, name_col]).agg({qty_col: 'sum', sales_col: 'sum'}).reset_index()
            summary_df = summary_df.sort_values(by=sales_col, ascending=False).reset_index(drop=True)
            summary_df.index = summary_df.index + 1

            # Metrics
            col1, col2, col3 = st.columns(3)
            col1.metric("รหัสสินค้าทั้งหมด", f"{len(summary_df):,} รายการ")
            col2.metric("จำนวนที่ขายได้รวม", f"{summary_df[qty_col].sum():,.0f} ชิ้น")
            col3.metric("ยอดรวมเงินทั้งสิ้น", f"{summary_df[sales_col].sum():,.2f} บาท")

            st.markdown("---")
            st.subheader("📈 10 อันดับรหัสสินค้าที่ยอดขายสูงสุด")
            st.bar_chart(summary_df.head(10).set_index(id_col)[sales_col])

            st.subheader("📋 ตารางรวมยอดขายแยกตามรหัสสินค้า")
            st.dataframe(summary_df, use_container_width=True)

            st.subheader("📅 จำนวนรายการสั่งซื้อในแต่ละวัน (เรียงจากออเดอร์เยอะที่สุด)")
            daily_count = df.groupby(date_col).size().reset_index(name='จำนวนรายการ')
            daily_count = daily_count.sort_values(by='จำนวนรายการ', ascending=False).reset_index(drop=True)
            daily_count.index = daily_count.index + 1
            st.dataframe(daily_count, use_container_width=True)
        else:
            st.warning("ไม่พบข้อมูลยอดขาย")

    # =========================================================
    # หน้าที่ 2: สต็อกสินค้า (ดึงไฟล์ สต็อกสินค้า)
    # =========================================================
    elif page == "📦 สต็อกสินค้าคงเหลือ":
        st.title("📦 ระบบตรวจสอบสต็อกสินค้าคงเหลือ")
        
        # เปิดไฟล์ "สต็อกสินค้า" และชีตย่อย "สินค้าคงเหลือ"
        sh_stock = gc.open("สต็อกสินค้า")
        ws_stock = sh_stock.worksheet("สินค้าคงเหลือ")
        data_stock = ws_stock.get_all_records()
        df_stock = pd.DataFrame(data_stock)

        if not df_stock.empty:
            # ⚠️ ตรวจสอบชื่อคอลัมน์ในชีตของคุณว่าชื่อ "จำนวนคงเหลือ" หรือไม่ ⚠️
            # ถ้าชื่ออื่นให้เปลี่ยนคำว่า 'จำนวนคงเหลือ' ในโค้ดด้านล่างให้ตรงกัน
            stock_qty_col = 'จำนวนคงเหลือ' 
            
            # แปลงค่าคงเหลือเป็นตัวเลข
            df_stock[stock_qty_col] = pd.to_numeric(df_stock[stock_qty_col].astype(str).str.replace(',', ''), errors='coerce').fillna(0)

            # กรองเฉพาะสินค้าที่เหลือน้อยกว่า 2
            low_stock_df = df_stock[df_stock[stock_qty_col] < 2].copy()
            low_stock_df = low_stock_df.reset_index(drop=True)
            low_stock_df.index = low_stock_df.index + 1

            # แสดงผล
            st.error(f"⚠️ พบสินค้าที่ต้องเติมด่วน (เหลือน้อยกว่า 2 ชิ้น): {len(low_stock_df)} รายการ")
            
            if not low_stock_df.empty:
                st.dataframe(low_stock_df, use_container_width=True)
            else:
                st.success("✅ ขณะนี้ไม่มีสินค้าที่เหลือน้อยกว่า 2 ชิ้น")
            
            st.markdown("---")
            st.subheader("📋 รายการสต็อกสินค้าทั้งหมด")
            # แสดงสต็อกทั้งหมดเผื่อคุณต้องการดู
            df_stock.index = range(1, len(df_stock) + 1)
            st.dataframe(df_stock, use_container_width=True)
        else:
            st.warning("ไม่พบข้อมูลในไฟล์สต็อกสินค้า")

except Exception as e:
    st.error(f"❌ เกิดข้อผิดพลาด: {e}")
    st.info("คำแนะนำ: ตรวจสอบว่าได้แชร์สิทธิ์ไฟล์ 'สต็อกสินค้า' ให้กับ Service Account (อีเมลใน key.json) หรือยัง?")
